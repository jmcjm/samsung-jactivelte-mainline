diff --git a/drivers/gpu/drm/msm/adreno/a3xx_gpu.c b/drivers/gpu/drm/msm/adreno/a3xx_gpu.c
index 0259e60b801..79ba16de869 100644
--- a/drivers/gpu/drm/msm/adreno/a3xx_gpu.c
+++ b/drivers/gpu/drm/msm/adreno/a3xx_gpu.c
@@ -6,6 +6,7 @@
  * Copyright (c) 2014 The Linux Foundation. All rights reserved.
  */
 
+#include <linux/delay.h>
 #include "a3xx_gpu.h"
 
 #define A3XX_INT0_MASK \
@@ -26,6 +27,7 @@
 extern bool hang_debug;
 
 #define A3XX_VBIF_XIN_HALT_CTRL0_MASK	GENMASK(5, 0)
+#define A3XX_VBIF_HALT_TIMEOUT_MS	100
 
 static void a3xx_dump(struct msm_gpu *gpu);
 static bool a3xx_idle(struct msm_gpu *gpu);
@@ -500,17 +502,31 @@ static u64 a3xx_gpu_busy(struct msm_gpu *gpu, unsigned long *out_sample_rate)
 
 static int a3xx_vbif_halt(struct msm_gpu *gpu)
 {
+	unsigned long timeout = jiffies + msecs_to_jiffies(A3XX_VBIF_HALT_TIMEOUT_MS);
 	u32 ack;
-	int ret;
 
 	gpu_write(gpu, REG_A3XX_VBIF_XIN_HALT_CTRL0,
 		  A3XX_VBIF_XIN_HALT_CTRL0_MASK);
-	ret = spin_until(((ack = gpu_read(gpu, REG_A3XX_VBIF_XIN_HALT_CTRL1)) &
-			  A3XX_VBIF_XIN_HALT_CTRL0_MASK) ==
-			 A3XX_VBIF_XIN_HALT_CTRL0_MASK);
+
+	/*
+	 * Bounded poll instead of spin_until(): that macro busy-waits for a
+	 * full second without sleeping. On the Galaxy S4 Active the halt is
+	 * never acknowledged, so with spin_until() every runtime suspend
+	 * burned a core for a second, failed with -EBUSY and was retried a
+	 * few milliseconds later, forever.
+	 */
+	do {
+		ack = gpu_read(gpu, REG_A3XX_VBIF_XIN_HALT_CTRL1);
+		if ((ack & A3XX_VBIF_XIN_HALT_CTRL0_MASK) ==
+		    A3XX_VBIF_XIN_HALT_CTRL0_MASK)
+			break;
+		usleep_range(50, 100);
+	} while (time_before(jiffies, timeout));
+
 	gpu_write(gpu, REG_A3XX_VBIF_XIN_HALT_CTRL0, 0);
 
-	if (ret)
+	if ((ack & A3XX_VBIF_XIN_HALT_CTRL0_MASK) !=
+	    A3XX_VBIF_XIN_HALT_CTRL0_MASK)
 		return -EBUSY;
 
 	return 0;
@@ -524,8 +540,21 @@ static int a3xx_pm_suspend(struct msm_gpu *gpu)
 		return -EBUSY;
 
 	ret = a3xx_vbif_halt(gpu);
-	if (ret)
+	if (ret) {
+		/*
+		 * Suspending without the VBIF drained hangs the GPU on the
+		 * first submit after resume (hangcheck every few seconds
+		 * with phoc as the offending task). If the halt is never
+		 * acknowledged on this hardware, keep the GPU powered instead
+		 * of retrying every few milliseconds: take a permanent
+		 * runtime PM reference so no further suspend is attempted.
+		 */
+		dev_warn_once(gpu->dev->dev,
+			      "%s: VBIF halt not acknowledged, keeping the GPU out of runtime PM\n",
+			      gpu->name);
+		pm_runtime_get_noresume(&gpu->pdev->dev);
 		return ret;
+	}
 
 	return msm_gpu_pm_suspend(gpu);
 }
