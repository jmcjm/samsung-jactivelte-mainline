diff --git a/drivers/power/supply/max77693_charger.c b/drivers/power/supply/max77693_charger.c
index 76c58b32a37..5e9cf6baf61 100644
--- a/drivers/power/supply/max77693_charger.c
+++ b/drivers/power/supply/max77693_charger.c
@@ -298,7 +298,12 @@ static int max77693_charger_get_property(struct power_supply *psy,
 
 static const struct power_supply_desc max77693_charger_desc = {
 	.name		= MAX77693_CHARGER_NAME,
-	.type		= POWER_SUPPLY_TYPE_BATTERY,
+	/*
+	 * This is the charger, not the cell: with POWER_SUPPLY_TYPE_BATTERY
+	 * upower treats it as a second, permanently empty battery and the
+	 * shell shows the average of the real gauge and 0 %.
+	 */
+	.type		= POWER_SUPPLY_TYPE_USB,
 	.properties	= max77693_charger_props,
 	.num_properties	= ARRAY_SIZE(max77693_charger_props),
 	.get_property	= max77693_charger_get_property,
